import styled from "styled-components";

export const Calculator = styled.div`
    margin: 3rem auto;
    width: 20rem;

    background: var(--background-color);

    border-radius: 0.4rem;
    border: 1px solid var(--menu-title);

    box-shadow: 0px 0px 10px 0px var(--menu-title);
`